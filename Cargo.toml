[package]
name = "chip8-rs"
version = "0.1.0"
edition = "2024"

[features]
default = ["audio"]
audio = ["dep:rodio"]

[dependencies]
byteorder = "1.5.0"
clap = { version = "4.5.60", features = ["derive"] }
crossterm = "0.29.0"
nom = "8.0.0"
rodio = { version = "0.22.2", optional = true }
