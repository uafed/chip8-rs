[package]
name = "chip8-rs"
version = "0.1.0"
edition = "2024"

[features]
default = ["audio"]
audio = ["dep:rodio"]

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
crossterm = "0.29.0"
rodio = { version = "0.22.2", optional = true }
